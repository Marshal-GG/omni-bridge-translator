// ─────────────────────────────────────────────────────────────────────────────
// TEMPLATE — copy this file to lib/core/config/app_config.dart and fill in values.
// Get values from: Google Cloud Console → APIs & Services → Credentials
// ─────────────────────────────────────────────────────────────────────────────

class AppConfig {
  /// Google OAuth 2.0 Web Client ID.
  /// Google Cloud Console → APIs & Services → Credentials
  /// → OAuth 2.0 Client IDs → "Web client (auto created by Google Service)"
  static const String googleClientId = '';
}
